meta {
  name: Find All User
  type: http
  seq: 3
}

get {
  url: {{base_url}}/users
  body: none
  auth: inherit
}

vars:pre-request {
  base_url: base_url
}

settings {
  encodeUrl: true
  timeout: 0
}
