meta {
  name: Create Single Chat
  type: http
  seq: 2
}

post {
  url: {{base_url}}/chats/single
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "user1Id": "{{user_id}}",
    "user2Id": "<user2-uuid>"
  }
}

settings {
  encodeUrl: true
}
