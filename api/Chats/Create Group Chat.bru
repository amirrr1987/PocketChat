meta {
  name: Create Group Chat
  type: http
  seq: 1
}

post {
  url: {{base_url}}/chats/group
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
  : 
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "title": "Kos",
    "ownerId": "{{user_id}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  sdf
}
