meta {
  name: Create Message
  type: http
  seq: 1
}

post {
  url: {{base_url}}/messages
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "senderId": "{{user_id}}",
    "singleChatId": null,
    "groupChatId": "{{group_id}}",
    "content": "Hello!"
  }
}

settings {
  encodeUrl: true
}
