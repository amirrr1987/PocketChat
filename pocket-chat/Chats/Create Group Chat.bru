meta {
  name: Create Group Chat
  type: http
  seq: 1
}

post {
  url: {{base_url}}/chats/group
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "title": "My Group",
    "ownerId": "{{user_id}}"
  }
}

settings {
  encodeUrl: true
}
